/** The five decode glyphs. Index into this array is the sequence alphabet. */
export const SYM = ['◈', '◇', '◉', '◎', '◊'];

/** The station's mark, used in the header and on buttons. */
export const MARK = '◈';
